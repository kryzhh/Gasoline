// Remember to enjoy the light before it's gone
 
#include "../core/networking/server.hpp" // Include server

// Main function of the program (Just launches server)

int main() {

    gasoline::Server server;
    server.start();

    return 0;
}